def reverse_string(input_string):
    return input_string[::-1] # here we are reversing the string using python slicing so we tell it to start at the end of the string and move backwards

def capitalize_string(input_string):
    return input_string.title()
